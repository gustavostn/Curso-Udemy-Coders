//var mensagem = sexo == 'M' ? "bem vindo senhor" : "bem vinda senhora";


function divisao(dividendo, divisor) {
    conta = dividendo / divisor
    resto = dividendo % divisor
    return conta && resto
}

console.log(divisao(6, 5));

